
Instructions
1. Run `amplify mock api`
2. Sign in as username: "awstesterlim" and groups: "HFSpecialists"
3. Run MyMutation
4. Run MyQuery, ClinicianInfo and ClinicianProtectedInfo should be shown
5. Sign in as username: "patienttest" and groups: "Patients"
6. Run MyMutation, should be unauthorised
7. Run MyQuery, ClinicianInfo should be shown, ClinicianProtectedInfo should be unauthorised

query MyQuery {
  getClinicianInfo(clinicianID: "awstesterlim") {
    clinicianID
    createdAt
    hospitalName
    id
    name
    owner
    protectedInfo {
      DTA
      UXSA
      APS
    }
  }
  getClinicianProtectedInfo(clinicianID: "awstesterlim") {
    id
    APS
    DTA
    UXSA
    _deleted
  }
}

mutation MyMutation {
  createClinicianInfo(
    input: {name: "Lim", hospitalName: "Hospital John", role: "HFSpecialist", clinicianID: "awstesterlim", owner: "awstesterlim"}
  ) {
    clinicianID
    createdAt
    hospitalName
    id
    name
    owner
    role
    updatedAt
    protectedInfo {
      APS
      DTA
      UXSA
    }
  }
  createClinicianProtectedInfo(
    input: {facts: "a", APS: "a", DTA: "a", UXSA: "a", owner: "awstesterlim", clinicianID: "awstesterlim"}
  ) {
    id
    facts
    createdAt
    clinicianID
    owner
  }
}
