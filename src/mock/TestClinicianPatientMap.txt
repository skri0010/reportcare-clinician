
Instructions
1. Go through `TestClinicianInfo.txt`
2. Sign in as username: "awstesterlim" and groups: "HFSpecialists"
3. Run MyMutation
4. Run MyQuery, ClinicianPatientMap, ClinicianInfo and ClinicianProtectedInfo should be shown
5. Sign in as username: "patienttest" and groups: "Patients"
6. Run MyMutation, should be unauthorised
7. Run MyQuery, ClinicianPatientMap and ClinicianInfo should be shown, ClinicianProtectedInfo should be unauthorised

query MyQuery {
  getClinicianPatientMap(clinicianID: "awstesterlim", patientID: "patienttest") {
    id
    clinicianInfo {
      id
      clinicianID
      createdAt
      hospitalName
      name
      owner
      protectedInfo {
        DTA
        UXSA
        APS
      }
    }
    clinicianID
    owner
    patientID
  }
}

mutation MyMutation {
  createClinicianPatientMap(
    input: {clinicianID: "awstesterlim", patientID: "patienttest", owner: "awstesterlim"}
  ) {
    id
  }
}
